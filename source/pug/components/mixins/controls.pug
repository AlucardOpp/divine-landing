mixin controls(currentItem, itemCount)
  .controls&attributes(attributes)
    +buttonArrow.btn--arrow.btn--arrow-dark.btn--arrow-left.controls__button.swiper-button-prev
    span.swiper-pagination= `${currentItem}/${itemCount}`
    +buttonArrow.btn--arrow.btn--arrow-dark.controls__button.swiper-button-next
